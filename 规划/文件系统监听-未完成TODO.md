# 文件系统监听与增量索引：未完成 TODO

> 对应规划：[文件系统监听-三阶段实施计划.md](./文件系统监听-三阶段实施计划.md)
>
> 当前状态：监听模块、事件转换、SQLite 增量写入和应用生命周期基础接入已完成；一致性、动态配置和生产级恢复仍未完成。

## 一、当前已完成

- [x] 新增独立 `file_watcher` 模块
- [x] 支持递归目录监听
- [x] 支持新增、修改、删除事件
- [x] 支持基础 rename 事件识别
- [x] 支持隐藏文件、排除目录和排除扩展名过滤
- [x] 支持 debounce 和批量事件合并
- [x] 新增 `FileIndexChange` 增量变更模型
- [x] 新增 `IndexSQL::apply_file_changes` 批量事务接口
- [x] 支持按路径 upsert、单路径删除、目录递归删除和 rename
- [x] 增加 `file_index.path` 唯一索引

## 二、第一阶段剩余任务：接入并跑通 MVP

### 2.1 应用生命周期接入

- [x] 在 `main.rs` 中初始化 `FileWatcher`
- [x] 使用 `app.manage` 持有 watcher 句柄，避免线程被提前释放
- [x] 防止重复启动 watcher（当前通过单次 setup 初始化）
- [x] 应用退出时停止监听线程
- [x] 监听初始化失败时不阻塞应用启动

### 2.2 事件到索引记录的转换

- [x] 抽取统一的“路径 -> `FileIndex`”函数
- [x] 复用全量扫描中的标题、类型、拼音和首字母生成逻辑
- [x] 对新增/修改路径判断文件是否仍存在
- [x] 对目录事件区分文件和目录
- [x] 处理 rename 事件的旧路径和新路径
- [x] 过滤无法读取或已经消失的路径

### 2.3 watcher 与 SQLite 串接

- [x] 通过 channel 将 watcher 批次发送给索引服务
- [x] 索引服务串行调用 `apply_file_changes`
- [x] 全量重建期间暂存增量事件
- [x] 全量重建结束后补处理暂存事件
- [x] 处理数据库事务失败并记录错误

### 2.4 MVP 验收

- [ ] 新建文件后可以被搜索（需联网解析依赖并实机验证）
- [x] 修改文件后不会产生重复记录（已有路径 upsert 单元测试）
- [x] 删除文件后搜索不到旧路径（已有删除事务单元测试）
- [ ] 批量复制文件时应用不明显卡顿
- [x] watcher 失败时日志包含明确原因
- [x] 手动重建仍可作为恢复手段

## 三、第二阶段剩余任务：稳定版

### 3.1 重命名和目录操作

- [ ] 完善不同平台的 rename 事件兼容
- [ ] 处理目录整体移动
- [ ] 处理目录删除及其子项索引清理
- [ ] 对 rename 无法确认的情况降级为 remove + rescan

### 3.2 过滤设置动态生效

- [ ] 将全量扫描和 watcher 共用同一套过滤器
- [ ] 保存排除路径后重载 watcher
- [ ] 保存排除文件类型后重载 watcher
- [ ] 新增排除规则后删除已有匹配索引
- [ ] 取消排除规则后扫描并补录允许路径
- [ ] 设置变化后执行一次索引校正

### 3.3 并发和状态管理

- [ ] 增加索引状态：Idle、Rebuilding、IncrementalUpdating、Stale
- [ ] 防止全量重建和增量事务并发写库
- [ ] 对同一路径只保留最终事件
- [ ] 限制单批事件数量，增加背压处理
- [ ] 为 watcher 增加状态查询或前端事件

### 3.4 稳定版验收

- [ ] 新建、复制、移动、重命名、删除文件和目录均能同步
- [ ] 全量扫描与增量监听过滤规则一致
- [ ] 修改设置后无需重启即可生效
- [ ] 重建期间发生的文件变化不会静默丢失
- [ ] Windows 本地目录完成实机验证
- [ ] macOS 基础场景完成实机验证

## 四、第三阶段剩余任务：生产级

### 4.1 异常恢复

- [ ] 处理 watcher 队列溢出
- [ ] 监听根目录失效时标记索引为 stale
- [ ] 异常后自动触发全量重建
- [ ] 增加重建冷却时间，避免反复重建
- [ ] 盘符、移动磁盘和网络路径恢复后自动重试

### 4.2 一致性与性能

- [ ] 启动时校验监听配置和索引状态
- [ ] 增加“校验索引”操作
- [ ] 统计事件积压量、批处理耗时和重建次数
- [ ] 优化批量事务和路径去重
- [ ] 对超大规模文件变更进行性能测试

### 4.3 可观测性和发布

- [x] 增加 watcher 启停、错误和恢复日志（基础日志）
- [ ] 前端展示监听状态和索引状态
- [ ] 增加关闭文件监听的配置开关
- [ ] 增加诊断信息导出
- [ ] 建立 Windows/macOS 发布前测试矩阵

## 五、依赖与验证阻塞项

- [ ] 在可联网环境解析并下载 `notify` 依赖
- [ ] 完成 `cargo check`
- [x] 完成 watcher 单元测试
- [x] 完成 SQLite 增量接口测试
- [ ] 完成 watcher 到数据库的集成测试
- [ ] 验证中文路径、长路径、大小写变化和临时文件行为

## 六、建议实施顺序

1. [x] 先完成 `main.rs` 接入和事件到 `FileIndex` 的转换
2. [ ] 再完成全量重建与增量写入的互斥
3. [ ] 接着处理排除设置动态重载和索引校正
4. [ ] 然后补齐 rename、目录移动和异常恢复
5. [ ] 最后完成跨平台测试、性能测试和前端状态展示
